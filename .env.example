# Required only for a genuine model-driven discovery run.
OPENAI_API_KEY=

# The approved default. Override only for an intentionally reviewed model change.
OPENAI_MODEL=gpt-5.6-sol

# Optional when a browser is installed outside Playwright's default cache.
PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=
